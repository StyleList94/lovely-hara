import IconDarkMode from './dark-mode';
import IconBrandGithub from './github';
import IconLightMode from './light-mode';

const Icon = {
  DarkMode: IconDarkMode,
  LightMode: IconLightMode,
  BrandGithub: IconBrandGithub,
};

export default Icon;
